[build]
  command = "npm run build"
  publish = "dist/aurelis/browser/browser"

# SPA fallback: let Angular Router handle client-side routes on refresh/direct links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
